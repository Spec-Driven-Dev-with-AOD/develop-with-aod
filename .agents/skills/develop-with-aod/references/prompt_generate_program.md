# Generate Program Invocation

Stage: `generate-program`

- AOD-YAML: `{{AOD_YAML_FILENAME}}`
- Companion context: `{{AOD_CONTEXT_FILENAME}}`
- Implementation profile: `{{AOD_IMPLEMENTATION_PROFILE_FILENAME}}`
- Experience brief and resources: `{{AOD_EXPERIENCE_FILENAME_AND_RESOURCE_FILENAMES_OR_NONE}}`
- Compatible additional directions: `{{ADDITIONAL_IMPLEMENTATION_DIRECTIONS_OR_NONE}}`

Apply `stage_generate_program.md`. Stop on any preflight condition defined there;
otherwise generate, verify, and report only the complete project and required
realization and traceability artifacts. After the stage response, append the
standard stage-completion menu required by `SKILL.md`.
